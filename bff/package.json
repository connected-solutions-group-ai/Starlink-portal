{
  "name": "csg-sales-portal-bff",
  "version": "1.0.0",
  "description": "Backend-for-Frontend for the CSG Sales Portal. Bridges the browser to NetSuite via REST Web Services + SuiteQL using OAuth 2.0 machine-to-machine (client credentials with a signed JWT assertion). The browser never talks to NetSuite directly and never holds NetSuite secrets.",
  "type": "commonjs",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
